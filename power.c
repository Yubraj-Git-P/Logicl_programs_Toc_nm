#include <stdio.h>
#include <math.h>
#define fx sqrt(1+x*x)

int main()
{
    int x;
    // float fx;

    printf("input for x : ");
    scanf("%d",&x);

    // fx = sqrt(1+x*x);
    
    printf("Value of x is %f \n",fx);  

    return 0;
}

#include <stdio.h>
#include <math.h>
#define fx sqrt(x*x-1)
int main()
{
    int x,fx;

    printf("Input for x : ");
    scanf("%d",&x);

    fx = sqrt()

    return 0;
}